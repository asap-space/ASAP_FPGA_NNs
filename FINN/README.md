# Usefull commands
./run-docker.sh notebook

## VAENet (WIP)
- FINN does not support AvgPool2d with non-squared kernels. The kernel used in this Neural Network is (4, 8). It throws the following error: `FINN only supports average pooling with 2D square kernels.`

## ESPERTA (WIP)
- I believe FINN does not support the Sigmoid layer. I am unsure of this tho.

## CNet
